angular.module("controllers", [])

.controller("MenubarController", function($rootScope, $scope, $location, $window, Notification) {
    $rootScope.title = "FS20";
    
})

.controller("MainController", function($scope) {
})
